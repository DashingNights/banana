const config = {
    Mongodb: {
        host: '127.0.0.1',
        name: 'PROD'
    },
    JWT: {
        token: 'secret'
    },
    Session: {
        token: 'token'
    }
};
module.exports = config;