const config = {
      Mongodb: {
        host: 'localhost',
        name: 'PROD'
      },
      JWT: {
        token: 'secret'
      },
      Session:{
        token: 'token'
      }
   };
module.exports = config;